import 'package:flutter/material.dart';
class GenerateBarCodeWithData extends StatelessWidget {
  const GenerateBarCodeWithData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('hello'),
      ),
    );
  }
}
